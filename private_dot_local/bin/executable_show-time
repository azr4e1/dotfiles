#!/usr/bin/env bash
bat=$(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null | head -n1)
notify-send -a clock -t 4000 \
    -h string:x-canonical-private-synchronous:clock \
    "$(date '+%H:%M')" "$(date '+%A %-d %B')${bat:+  |  battery ${bat}%}"
